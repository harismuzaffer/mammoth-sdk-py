"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from mammoth_analytics.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List
from typing_extensions import NotRequired, TypedDict


class RruleSchemaFrequency(str, Enum):
    r"""frequency"""

    MINUTELY = "minutely"
    HOURLY = "hourly"
    DAILY = "daily"
    WEEKLY = "weekly"
    MONTHLY = "monthly"
    YEARLY = "yearly"


class RruleSchemaTypedDict(TypedDict):
    interval: int
    r"""interval"""
    frequency: RruleSchemaFrequency
    r"""frequency"""
    start: datetime
    r"""start"""
    by_week_day: NotRequired[Nullable[List[str]]]
    r"""by_week_day"""
    by_month_day: NotRequired[Nullable[List[int]]]
    r"""by_month_day"""


class RruleSchema(BaseModel):
    interval: int
    r"""interval"""

    frequency: RruleSchemaFrequency
    r"""frequency"""

    start: datetime
    r"""start"""

    by_week_day: OptionalNullable[List[str]] = UNSET
    r"""by_week_day"""

    by_month_day: OptionalNullable[List[int]] = UNSET
    r"""by_month_day"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["by_week_day", "by_month_day"]
        nullable_fields = ["by_week_day", "by_month_day"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
